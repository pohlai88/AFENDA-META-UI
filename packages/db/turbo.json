{
  "extends": ["//"],
  "tags": ["data"],
  "tasks": {
    "db:push": {
      "env": ["DATABASE_URL"],
      "cache": false
    },
    "db:generate": {
      "env": ["DATABASE_URL"],
      "outputs": ["migrations/**"]
    },
    "test:db": {
      "env": ["DATABASE_URL"],
      "outputs": ["coverage/**"]
    },
    "truth:check": {
      "outputs": []
    }
  }
}

/**
 * App Entry Point
 * ================
 * Now using react-router-dom for navigation with AppShell layout.
 */

import React from "react";
import { AppRouter } from "./routes";

export default function App() {
  return <AppRouter />;
}
